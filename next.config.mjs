/** @type {import('next').NextConfig} */
// Removed invalid experimental `turbo` object. Next handles Turbopack
// via CLI/flags and built-in defaults; keeping a plain config to avoid
// the "invalid experimental key" startup message.
const nextConfig = {
  // Add any supported top-level Next.js config here
}

export default nextConfig